# Provider API keys. Copy this file to .env (which is gitignored) and fill in.
# Do NOT commit .env. The pre-commit gitleaks hook is the safety net.

ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GOOGLE_API_KEY=
